# 학원 밖(집 / 핫스팟)에서 DB에 붙을 때 쓰는 개인 설정.
#
# 1) 이 파일을 복사해서 같은 폴더에  db_local.properties  로 저장
# 2) db.host 에 학원 DB 서버의 공인 IP 를 적는다 (팀 채팅에서 확인)
#
# db_local.properties 는 .gitignore 되어 있어 커밋되지 않습니다.
# 공인 IP 를 public 저장소에 올리지 않기 위한 것이니 그대로 두세요.
# 학원에서는 이 파일이 없어도 됩니다 - jsl-704 로 먼저 붙습니다.
db.host=
