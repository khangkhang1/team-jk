build/
.settings/
*.class
/bin/

# 개인 DB 접속 설정 (공인 IP) - 저장소에 올리지 않음
src/main/java/db_local.properties

# 비밀값(DB 계정, 공공데이터 서비스키, 메일 앱 비밀번호) - 저장소에 절대 올리지 않음
# 각자 secret.properties.example 을 복사해서 만든다. common/SecretConfig.java 참고.
src/main/java/secret.properties
